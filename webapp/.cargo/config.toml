# cargo 调用的进程（构建脚本 + cargo run 出来的二进制）默认带上这些环境变量。
# 已在外部 shell 显式设置的同名变量不会被覆盖。
# 注意：这个文件会被提交进仓库，不要在这里写任何机器私有的绝对路径。
[env]
# babycat -> libsamplerate-sys 的 CMakeLists 兼容新版 CMake 所需
CMAKE_POLICY_VERSION_MINIMUM = "3.5"
